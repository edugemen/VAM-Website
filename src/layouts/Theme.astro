<script is:inline>
    const setTheme = () => {
        let theme = localStorage.getItem("theme") || "light";
        document.documentElement.setAttribute("data-theme", theme);
    };
    setTheme();
</script>
